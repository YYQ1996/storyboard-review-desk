# M1.10 · 双平台整合包

- 状态：部分完成（自动与本机验证通过，待 Windows 干净电脑和 Mac 真机验收）
- 验收日期：2026-09-13
- 对应计划：[迭代计划](../迭代计划.md)
- 交付版本：`v0.1.12`

## 本节点交付物

- 将分类资产索引板、不中断批注输入和 YIQI玩AI 品牌标识合并为统一版本。
- 生成 Windows 便携安装包与 macOS Apple Silicon/Intel 通用安装包。
- 新版启动器识别同端口运行版本，避免升级后误打开仍在运行的旧版。
- 安装说明补充旧项目 `data` 迁移、插件自动覆盖和新任务生效边界。

## 验收结果

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 版本一致性 | 通过 | 正式健康接口、插件清单、安装器、测试用例和使用说明统一为 `0.1.12` |
| 自动测试 | 通过 | `STORYBOARD_INSTALL_E2E=1` 全量执行 43 条，43 通过、0 失败、0 跳过 |
| Windows 安装包 | 通过 | `分镜审核台-v0.1.12-Windows.zip`；隔离用户目录安装、无 PATH 桌面 App 发现、失败窗口停留全部通过；SHA256 `94C473AFC2CE256663DCA6C6B55364EA941D6DE1E1564E63C44C047E3FFEA13A` |
| macOS 安装包 | 结构通过、待真机 | `分镜审核台-v0.1.12-macOS-universal.zip`；包含 arm64 与 x64 Node，脚本和运行文件权限校验通过；SHA256 `38EF4D7FA02888D11482D7B13425269F1540A5F469177D403F1C269216961704` |
| 隐私数据排除 | 通过 | 两个 ZIP 均不包含 `data/`；均包含品牌图标与新版插件 |
| 个人插件 | 通过 | 当前电脑通过 Windows 成品安装器刷新；缓存仅为无后缀 `0.1.12`，图标与技能文件存在 |
| 正式服务与数据 | 通过 | `http://127.0.0.1:43127/api/health` 返回 `ok=true, version=0.1.12`；原项目仍为 39 镜、1 批、7 项资产，最近一次运行保持 completed |
| GitHub 发布隔离 | 通过 | 本节点不执行 GitHub 推送，等待用户明确指令 |

## 自动测试

- 命令：启用 `STORYBOARD_INSTALL_E2E=1` 后执行完整 Node 测试套件。
- 结果：43 条全部通过；其中 3 条从 Windows 成品包在临时学员目录调用真实 `cmd.exe`。

## 人工验证

- 当前电脑已使用 Windows 成品安装器刷新个人插件，并重启正式服务到 `0.1.12`。
- 正式服务重启前后项目内容保持一致，最近一次 6 项资产压缩为 5 张索引板的完成记录仍可读取。
- macOS 包完成结构验证后仍需 Mac 真机确认 Gatekeeper 打开和插件安装流程。

## 已知限制

- 工具包按版本使用独立目录，旧项目需要复制旧包中的 `data` 文件夹到新包。
- macOS 包未使用 Apple Developer 证书签名，首次打开需要右键或按住 Control 选择“打开”。

## 下一里程碑

- 是否允许进入下一节点：否，等待用户在旧版 Windows 环境和 Mac 真机完成升级验收。
- 下一步：用户按新版说明复制旧 `data`、运行一键安装并新建 Codex 工作任务；全部通过后再按用户指令发布 GitHub。
