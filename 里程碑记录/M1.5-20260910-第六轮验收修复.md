# M1.5 · 第六轮验收修复

- 状态：部分完成
- 验收日期：2026-09-10
- 对应计划：../迭代计划.md

## 本节点交付物

- 非悬浮场景组标题和父级场景保存修复。
- 固定高度批注汇总区与宫格覆盖层尺寸监听。
- 批次权威范围归一、旧镜号缺口和跨批重号迁移修复。
- 新运行上下文快照、历史图片内容重复拦截。
- 结构化运行日志及页面“导出排查日志”入口。
- `0.1.6` 本地服务和个人 Codex 插件。

## 验收结果

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 场景父级解析 | 通过 | 用户附件解析为 39 镜，全部场景为“卧室-床区-深夜”；新增保存回归测试通过 |
| 当前数据镜号诊断 | 通过 | 确认旧数据总池缺少 #9、第二批 #10-#17 与第三批 #17 重叠 |
| 镜号迁移 | 通过 | 隔离副本迁移后总池为 #1-#39；第二批 #9-#16、第三批 #17-#25 |
| 旧图复用诊断 | 通过 | 第二批 v1 与第三批 v1 的 SHA-256 完全相同 |
| 旧图回填拦截 | 通过 | 新增自动测试证明不同运行提交同内容图片会被拒绝且不创建版本 |
| 批注与框选 | 待人工复验 | 汇总区固定高度；覆盖层由 ResizeObserver 跟随图片实际尺寸 |
| 运行日志 | 通过 | 服务记录关键运行事件，页面可导出最近 500 条 |
| 正式数据迁移 | 通过 | 正式项目已升级到 schema v3；总池连续 #1-#39，第二批 #9-#16、第三批 #17-#25；迁移前文件已备份 |
| 正式服务 | 通过 | `http://127.0.0.1:43127/api/health` 返回版本 `0.1.6`；日志接口已产生 `context_read` 记录 |
| 个人插件 | 通过 | 个人市场、安装清单和唯一缓存目录均为无后缀 `0.1.6`，安装后技能文件可直接读取 |

## 自动测试

- `node --test --test-isolation=none tests/*.test.mjs`
- 结果：25 项通过，0 项失败。
- 页面内联脚本：5 段语法检查通过。
- 技能校验：通过。
- 插件清单校验：通过。
- 正式服务页面静态核验：存在日志导出、固定高度批注汇总和 `ResizeObserver`；不存在悬浮场景标题。

## 人工验证

- 待用户重新导入附件脚本，检查场景标题及首批默认勾选。
- 待用户新增/删除定位批注，检查底部汇总和宫格框选。
- 待用户使用新任务实际生成一张新宫格，确认不会复用历史图片。

## 数据备份

- 迁移前正式项目：`../data/backups/project-before-m1.5-20260910.json`
- 当前正式项目：schema v3、revision 799。

## 已知限制

- 历史数据中已经丢失的场景名称无法仅靠无场景的镜头正文可靠推断；重新导入原脚本后可保留场景。既有批次的镜号缺口和重号可以自动修复。
- 当前仍只允许最新批次改变镜头数量；已有后续批次时继续阻止改变前批镜数。

## 下一里程碑

- 是否允许进入下一节点：否。
- 下一步：完成 M1.5 人工验收后，再评估 M2 一键创建 Codex 任务。
