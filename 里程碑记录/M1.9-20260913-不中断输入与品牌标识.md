# M1.9 · 不中断输入与品牌标识

- 状态：部分完成（待用户交互与视觉复验）
- 验收日期：2026-09-13
- 对应计划：[迭代计划](../迭代计划.md)
- 发布策略：仅保存在本地，随下一次正式版本统一更新 Windows、macOS 与 GitHub

## 本节点交付物

- 后台回填刷新前保存审核批注输入值、光标选区和输入框滚动位置，刷新后恢复焦点与草稿。
- 使用用户提供的小人 IP 生成“小人骑木马＋四格分镜”品牌图标，输出 512×512 透明 PNG。
- 审核台顶栏、浏览器页签和空状态接入共用图标；顶栏弱提示“by YIQI玩AI”，空状态展示“YIQI玩AI · 出品”。
- Codex 插件清单补充 `composerIcon`、`logo`、`logoDark`、品牌色和开发者名称。
- 当前电脑的个人插件已通过卸载后重装刷新，并保持无后缀 `0.1.11`；未修改公开 `v0.1.11` Release。

## 验收结果

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 输入状态保护 | 自动通过 | `TC-118` 验证捕获发生在工作区重绘前、恢复发生在重绘后，并恢复焦点与光标选区 |
| 审核台图标 | 自动通过、待视觉复验 | `/assets/brand-icon.png` 返回 HTTP 200，尺寸 512×512，四角 Alpha 为 0；顶栏、favicon 与空状态均引用同一资源 |
| 插件图标 | 自动通过、待 Codex UI 复验 | 插件清单三项图标字段均指向实际存在的 `./assets/brand-icon.png` |
| 开发者标识 | 自动通过 | 页面和插件清单均为“YIQI玩AI” |
| 个人插件 | 通过 | 本地缓存版本 `0.1.11` 已重新安装并出现在插件清单中，未保留临时缓存后缀 |
| GitHub 发布隔离 | 通过 | 本节点未执行 GitHub 推送，也未生成或覆盖 `v0.1.11` 双平台安装包 |

## 自动测试

- 命令：`node --test --test-isolation=none tests/*.test.mjs`
- 结果：`39 passed, 0 failed, 3 skipped`；3 项为需要发布包隔离环境变量的安装端到端用例。
- 插件清单校验：通过。
- 技能校验：通过。
- 正式服务图标资源：HTTP 200，`image/png`。

## 人工验证

1. 在一个审核中批次的“审核批注”输入框持续输入，将光标停在文字中间。
2. 等待另一个批次完成回填并出现 Toast，确认输入框仍保持焦点，继续输入不会落到页面其他位置。
3. 打开审核台，确认左上角图标和“YIQI玩AI”标识不挤占导入、导出等操作。
4. 完全退出并重开 Codex，新建任务后查看分镜审核台插件图标。

## 已知限制

- 当前环境无法通过自动化浏览器连接完成页面截图，因此页面布局和 Codex 插件卡片需要用户视觉复验。
- 当前本地代码仍沿用 `0.1.11` 功能版本；下一次正式发布时再统一升级为新的无后缀版本号。

## 遗留风险

- Codex 可能需要完整退出并重新打开后才刷新插件图标；旧任务不会自动重新加载插件界面元数据。

## 下一里程碑

- 是否允许进入下一节点：否，等待用户完成 `TC-118` 与 `TC-119` 人工复验。
- 下一步：复验通过后保留为下次正式版本变更，届时统一生成 Windows、macOS 安装包并推送 GitHub。
