# M1.6 · 第七轮验收修复

- 状态：部分完成
- 验收日期：2026-09-10
- 对应计划：../迭代计划.md

## 本节点交付物

- 已创建批次冻结、待分配池衔接和终版统一校号。
- 最新批次删除及版本、运行记录清理。
- 已有项目的分镜脚本导入锁定。
- “导出操作记录”用户化命名。
- `0.1.7` 本地服务和个人 Codex 插件。

## 验收结果

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 跨批增减不阻断 | 通过 | 已删除旧的后续批次阻断逻辑；通过操作只固化当前批版本，不再改写总池或其他批次 |
| 批次源镜头冻结 | 通过 | 服务端保存 `sourceShots`；自动测试证明总池改号后已创建批次上下文仍保持原镜号 |
| 终版校号 | 通过 | 自动测试构造跨批重复镜号，导出结果按批次顺序连续编号为 `1..N` |
| 最新批次删除 | 通过 | 服务端拒绝删除非最新批次；删除最新批次后同步移除该批运行记录，版本图片只删除未被其他批引用的文件 |
| 导入锁定 | 待人工复验 | 项目已有脚本或批次时顶部导入按钮禁用；动作入口和提交入口均有二次保护 |
| 操作记录命名 | 通过 | 顶部按钮、下载文件名和提示统一改为“操作记录” |
| 正式数据迁移 | 通过 | 当前项目升级到 schema v4、revision 944；39 镜和5个现有批次均保留，迁移前另存备份 |
| 正式终版导出 | 通过 | 当前真实数据检测到跨批镜号异常；导出副本自动生成连续 #1-#39，页面历史批次未被改写 |
| 正式服务 | 通过 | `/api/health` 返回 `ok=true, version=0.1.7`；页面不存在旧阻断提示和“导出排查日志”文案 |
| 个人插件 | 通过 | 个人市场、安装清单和唯一缓存目录均为无后缀 `0.1.7` |

## 自动测试

- `node --test --test-isolation=none tests/*.test.mjs`
- 结果：28 项通过，0 项失败。
- 页面内联脚本：5 段语法检查通过。
- 服务端脚本语法检查：通过。
- 插件清单校验与技能快速校验：通过。

## 数据备份

- 迁移前正式项目：`../data/backups/project-before-m1.6-20260910.json`
- 当前正式项目：schema v4、revision 944。

## 人工验证

- 待用户让已有后续批次的旧批发生镜头合并或新增，再确认可以通过且后续批次保持不变。
- 待用户创建下一批，确认待分配镜号从最新批次末镜之后连续衔接。
- 待用户验证有图与无图两种最新批次删除提示，以及已有项目的导入按钮锁定。

## 下一里程碑

- 是否允许进入下一节点：否。
- 下一步：完成 M1.6 页面验收后，再评估 M2 一键创建 Codex 任务。
