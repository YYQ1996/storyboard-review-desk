# M1.3 · 第四轮验收修复

- 状态：部分完成（开发、自测与发布完成，待用户真实生图耗时/效果复验）
- 验收日期：2026-09-10
- 对应计划：[迭代计划](../迭代计划.md)
- 完整用例：[测试用例](../测试用例.md)

## 本节点交付物

- 新建批次双列卡片布局与 9 镜 / 15 秒默认勾选。
- 新增返修脚本确认接口和 MCP 工具 `prepare_revision_script`。
- 回填版本改为使用运行中已确认的权威脚本，杜绝图变脚本不变。
- 技能压缩为最短调用链，生图提示只能原样使用 `imageScript`。
- 个人插件使用无构建后缀的正式版本 `0.1.4`。
- 页面给出 `GPT-5.6 Luna · 低推理` 的默认模型建议。

## 验收结果

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 双列镜头卡片 | 通过 | Codex 内置浏览器 844px 宽度实测：同屏每行两张卡片，字段仍完整显示 |
| 默认勾选 | 通过 | 正式数据实测自动选择 #19–#27，共 9 镜、12.7 秒；#28 未选 |
| 15 秒 / 9 镜算法 | 通过 | `defaultBatchShotNos` 自动测试覆盖超时提前停止和 9 镜上限 |
| 返修脚本必须变化 | 通过 | 自动测试验证原脚本被拒绝，镜号变化被服务端契约禁止 |
| 返修图文同源 | 通过 | 自动测试验证准备后的“人物缓慢抬头 / 情绪目标：克制”写入新版本 `rawText` |
| 生图输入无额外创作提示 | 规则通过 | 技能要求 `prompt` 逐字等于 `imageScript`，禁止附加意见、正负向提示、解释和生成后检查 |
| 删除冗余检查 | 规则通过 | 技能不再扫描环境/插件路径/项目图片，不预览或检查生成结果；参考图仅一次并行读取 |
| 插件路径一致 | 通过 | `codex plugin list` 显示 `0.1.4`；唯一缓存目录为 `.../storyboard-review-desk/0.1.4`，当前 Codex 技能清单也直接从该无后缀路径加载技能 |
| 正式服务 | 通过 | `/api/health` 返回 `ok=true, version=0.1.4`；正式数据仍为 36 镜头、2 批次、7 资产、2 运行记录 |
| 真实返修耗时 | 待用户复验 | 需要新任务使用 Luna 低推理跑一次真实内置生图，记录总耗时并确认页面脚本字段变化 |

## 自动测试

- `node --test --test-isolation=none tests/*.test.mjs`：20 项通过，0 项失败。
- 插件校验：通过。
- 服务端和插件 MCP 服务语法检查：通过。

## 人工验证

1. 正式服务重启到 `0.1.4` 并刷新验收页面。
2. 只打开“新建批次”检查布局和默认值，未点击创建，未修改正式项目数据。
3. 当前 844px 页面显示双列镜头卡；底部显示“已选 9 个 · 共 12.7 秒：#19–#27”。
4. 个人插件同步并重新安装后，缓存根目录仅有 `0.1.4`。

## 已知限制

- 内置图片生成本身的排队和生成耗时不受审核台控制；本次目标是把图片生成之外的步骤压缩到必要最小值。
- Luna 低推理是默认建议，不是强制模型。若返修脚本质量不稳定，可切换 Terra 低推理，不应先恢复冗长提示词或生成后检查。
- 当前任务不会热加载刚安装的插件；真实复验必须新建 Codex 任务。

## 下一里程碑

- 是否允许进入下一节点：否
- 下一步：用户按 TC-050B/C/D 在新任务完成一次真实返修；确认耗时、纯净生图输入和脚本变化后，追加 M1.3 通过记录。

## 发布检查清单

- 正式验收地址：`http://127.0.0.1:43127/`，健康版本 `0.1.4`。
- 个人插件：`storyboard-review-desk@personal` `0.1.4`，installed、enabled，无缓存后缀。
- 文档：README、测试用例、迭代计划和本里程碑记录已同步。
- 正式数据：发布与页面验证未创建批次、未删除或改写资产。
