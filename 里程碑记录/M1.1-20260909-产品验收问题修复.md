# M1.1 · 产品验收问题修复

- 状态：部分完成（开发与自测完成，待用户复验）
- 验收日期：2026-09-09
- 对应计划：[迭代计划](../迭代计划.md)
- 完整用例：[测试用例](../测试用例.md)

## 本节点交付物

- 顶部 Toast 和导入成功后自动打开首批创建窗口。
- 新建批次长文本完整展示。
- 人物、场景、道具三类参考资产，以及新上传资产默认选中。
- 全部参考资产被选中时的自动展开、高亮和二次确认。
- 分镜卡片五类字段独立展示与旧项目字段迁移。
- `16:9` / `9:16` 成片画幅选择及后端、插件上下文传递。
- 宫格画幅、`#镜号` 和底部台词/心声/音效的固定生成约束。

## 验收结果

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| Toast 顶部显示 | 通过 | 隔离页面导入成功后实测 Toast 距页面顶部 20px |
| 导入后自动新建批次 | 通过 | 隔离页面导入 2 镜脚本后，新建批次弹窗自动打开 |
| 新建批次长文本 | 通过 | 长构图、长画面、声音和情绪目标均完整换行显示，无截断样式 |
| 道具资产 | 通过 | 页面出现道具入口和道具分组；自动测试确认 MCP 上下文返回 `type=prop` |
| 分镜字段结构 | 通过 | 隔离页面和现有 41 镜项目均显示独立字段；旧版 `rawBlock` 迁移测试通过 |
| 上传后默认勾选 | 通过（代码与状态逻辑） | 上传处理将新资产 ID 自动加入当前批 `assetIds`；待用户做一次实际文件上传复验 |
| 全选资产防误操作 | 通过 | 隔离页面两张资产全选时自动展开、高亮并显示“确认使用全部”；取消后可删减为一张继续 |
| 成片画幅 | 通过 | 默认 16:9；隔离页面切换 9:16 后，`/api/bootstrap` 持久化为 9:16 |
| 宫格标记与声音文字 | 通过（提示词约束） | 首版与返修提示词均要求每格所选画幅、左上角仅 `#镜号`、底部完整声音字段 |
| Codex 插件契约 | 通过 | MCP 真实读取返回 4 个工具、`aspectRatio=9:16`、唯一道具资产和五个结构化镜头字段；个人插件已安装启用 |

## 自动测试

- 命令：`bun run test`
- 结果：14 项通过，0 项失败。
- 新增覆盖：结构化字段解析与导出、旧版 `rawBlock` 迁移、道具资产和 9:16 上下文。
- 语法检查：页面内联脚本、本地服务、数据存储、解析器与插件 MCP 服务全部通过。
- 插件版本：`storyboard-review-desk@personal` `0.1.1+codex.20260909065608`，状态为 `installed, enabled`。
- MCP 实测：通过独立运行令牌读取到当前批唯一道具图、9:16 画幅、五个结构化镜头字段，隔离提示包含道具约束。

## 人工与隔离页面验证

1. 在独立临时数据目录导入两镜长文本，确认顶部 Toast、自动批次弹窗和完整文字。
2. 注入一张人物图和一张道具图并全部选中，点击“仅复制提示词”。
3. 页面自动展开资产区并黄色高亮，显示可取消或“确认使用全部”的二次确认。
4. 取消后移除人物图，只保留道具图；切换 9:16，后端状态保存 `aspectRatio=9:16`、当前批只含道具资产。
5. 正式项目只做结构化迁移与页面读取验证，未清空、替换或注入测试业务数据。

## 已知限制

- “上传后默认勾选”已完成实现和代码检查，但浏览器自动选择本地文件受验收环境限制，保留一条用户实际上传复验用例。
- 图片模型是否严格把每格画成指定比例以及文字是否完全准确，仍需使用真实 Codex 生图结果做最终视觉验收；本节点先保证提示词、上下文和页面链路正确。
- M1 的人工新建独立 Codex 任务步骤未改变，一键创建任务仍属于 M2。

## 下一里程碑

- 是否允许进入下一节点：否
- 下一步：用户按更新后的《测试用例》复验 P0/P1；通过后追加 M1.1 通过记录，再评估 M2。
