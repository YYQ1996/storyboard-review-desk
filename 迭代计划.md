# 分镜审核台 · 迭代计划

> 当前路线：本地单用户工具，先跑通审核台与 Codex 的双向回填，再逐步增加一键任务编排和插件发布能力。
>
> 基线日期：2026-09-08

## 1. 产品目标

用最小成本完成以下闭环：

```text
审核台创建批次
  → 为该批次建立独立 Codex 任务
  → Codex 只读取本批剧本和本批选中的人物/场景/道具资产
  → 调用内置图片生成能力生成一张分镜宫格图
  → Codex 将图片和分镜文本回填审核台形成新版本
  → 用户审核、批注、返修或手动通过
```

最终产品可以进一步由审核台一键创建和控制 Codex 任务，但第一版不把这一高风险能力作为基础闭环的前置条件。

## 2. 已确认原则

- 运行形态：Windows 本地、单用户，只监听 `127.0.0.1`。
- 数据形态：元数据落盘，图片以文件保存，不再把大量 DataURL 写入 `localStorage`。
- 生图能力：调用 Codex 内置图片生成能力，不要求 OpenAI API Key。
- 隔离单位：每个批次使用独立 Codex 任务，不同批次不共享任务。
- 同批返修：允许复用该批次任务；系统不主动重新附带历史宫格图。
- 参考范围：只提供当前批次选择的人物图、场景图、道具图和本次文本。
- 审核权限：自动生成和自动回填；通过、覆盖已通过版本、清空项目继续由用户手动操作。
- 兼容策略：首次启动自动迁移旧 `localStorage` 数据，迁移后保留原数据作为回退副本。
- 交付策略：先完成个人插件，再评估公共插件市场上架。

## 3. 成本与范围控制

| 能力 | 难度/风险 | 安排 |
| --- | --- | --- |
| 本地数据与图片持久化 | 中 | 第一版必须完成 |
| 审核台向 Codex 提供批次上下文 | 中 | 第一版通过 MCP 工具完成 |
| Codex 自动提交生成版本 | 中偏高 | 第一版通过 MCP 回填工具完成 |
| 每批独立任务 | 低 | 第一版由用户手动新建任务保证 |
| 页面一键创建 Codex 任务 | 高 | M2 接入 Codex App Server |
| Codex 实时事件流 | 中偏高 | M2 再做，第一版使用状态轮询 |
| WebMCP 页面操作 | 中，且与插件重叠 | 暂缓 |
| SQLite 和复杂事务 | 中 | 暂缓，先使用原子 JSON |
| 公共插件市场上架 | 高，依赖外部审核 | M4 处理 |
| 多用户、云端同步、账号系统 | 高 | 不在当前路线内 |

## 4. 里程碑

### M0：Codex 双向通信技术验证

目标：用一个最小两镜头批次验证最不确定的链路，不先改造全部页面。

范围：

1. 建立最小本地数据目录和测试批次。
2. Codex 通过自建 MCP 工具取得批次文本和资产路径。
3. Codex 在同一任务中调用内置图片生成能力。
4. Codex 调用回填工具提交图片路径和分镜文本。
5. 回填工具将图片复制到项目目录并创建版本记录。

通过标准：

- MCP 工具可以读取正确批次，且不会返回其他批次数据。
- 内置图片生成能力可在插件工作流中调用。
- 生成图片最终位于项目目录，而非只留在 Codex 默认生成目录。
- 回填后能从本地状态中读取到一个完整版本。
- 失败时不产生半成品版本，并能再次提交。

风险闸门：

- 若内置生图无法在插件工作流中稳定触发，保留明确的人工生图步骤。
- 若图片路径无法直接交给 MCP 工具，降级为用户选择生成图片后半自动回填。
- 降级不改变数据模型和后续接口。

### M1：可跑通的本地 MVP

目标：完成可日常使用的“手动新建 Codex 任务 + 自动双向回填”闭环。

范围：

#### 本地服务

- 托管审核台页面和静态文件。
- 提供项目、批次、资产、运行和版本接口。
- 使用 `data/project.json` 保存元数据。
- 使用 `data/assets/`、`data/versions/` 保存图片。
- 所有状态写入先写临时文件，再原子替换正式文件。
- 串行处理写请求，避免页面和插件同时覆盖数据。

#### Codex 个人插件

第一版只开放四个工具：

- `get_batch_context`：读取本批剧本、返修意见和选中资产。
- `submit_storyboard_version`：提交生成图片、分镜文本和宫格布局。
- `report_generation_status`：回写准备、生图、回填和失败状态。
- `get_batch_status`：查询当前版本与审核状态。

插件不得提供自动通过、清空项目、删除历史版本或覆盖已通过版本的工具。

#### 页面流程

1. 用户导入剧本并创建批次。
2. 用户选择本批人物、场景和道具资产，并确认最终视频画幅。
3. 点击“创建 Codex 任务指令”。
4. 审核台创建带随机令牌的运行记录并复制指令。
5. 用户在同一项目下新建 Codex 任务并粘贴指令。
6. Codex 取得本批上下文、调用内置生图并提交版本。
7. 页面轮询运行状态并自动显示新版本。
8. 用户继续批注、返修或通过。

#### 前端必须修复

- 标准格式多行字段被截断。
- 竖线格式遗漏“机位”列。
- 首次只回填图片时镜头分配丢失。
- 宫格容量与镜头数量不匹配时缺少校验。
- 无真实图片或解析失败仍可通过。
- 历史版本仍然可被修改或用于错误返修。
- 返修意见来源版本含义错误。
- 空项目自动载入演示数据。
- 图片 DataURL 占满 `localStorage`。

通过标准：

- 服务重启后，项目、资产、批次和版本仍可恢复。
- 旧页面数据首次进入时自动迁移，旧 `localStorage` 不被删除。
- 两个不同批次产生不同运行令牌，插件不能交叉读取。
- Codex 提交版本后，页面无需手动粘贴即可看到新图和文本。
- 同一提交重复调用不会创建两个版本。
- 历史版本只读；通过和清空操作仍需页面人工确认。
- 手动复制提示词与手动回填仍可作为降级入口使用。

### M1.1：产品验收问题修复

目标：修复首次产品验收中 P0/P1 问题，并以低成本处理不阻断主链路的 P2 可读性问题。

范围：

- 所有 Toast 移至页面顶部；导入解析成功后自动打开“新建批次”。
- 新建批次完整展示长镜头文字，不再只显示开头。
- 参考资产增加道具图；上传后默认勾选到当前批次。
- 全部资产被选中时，创建任务或复制提示词前自动展开、高亮并二次确认，同时允许用户确认确实使用全部。
- 分镜卡片将机位、构图、画面内容、台词/心声/音效、情绪目标拆为独立字段，并兼容旧数据迁移。
- 增加 `16:9` / `9:16` 成片画幅选择，默认 `16:9`，随批次上下文传给 Codex。
- 宫格提示词要求每格使用所选画幅、左上角只显示 `#镜号`，底部完整显示台词/心声/音效。

通过标准：

- P0/P1 对应交互和数据链路均有自动测试或隔离页面验证证据。
- 旧项目打开后镜头字段可重新结构化，镜头 ID 与原始文本不丢失。
- 插件能够读取画幅、道具资产和新的宫格文字约束。
- 用户完成本轮复验前，M1 与 M1.1 均不标记“已通过”。

### M1.2：第二轮验收问题修复

目标：前移成片画幅和图片质量校验，减少批次信息缺失与 Codex 任务运行后才失败的问题。

范围：

- 调整“剧本转分镜脚本”技能规范，要求每镜输出具体、稳定的场景字段，并按审核台支持的字段格式给出示例。
- 成片画幅在首次导入分镜脚本时选择，默认 `16:9`；导入成功后锁定，只在顶部栏展示。
- 新上传资产仅短暂高亮，不再自动进入重命名状态；仍保留用户主动点击名称重命名的能力。
- 浏览器上传阶段实际解码图片像素，并将通过校验的图片统一转码（M1.2 当时采用 PNG；已在 M1.2.1 改为更保守的 JPEG 兼容包络），规避异常压缩流、错误扩展名和不兼容元数据。
- 服务端再次严格校验 PNG/JPEG/WebP/GIF 的文件结构；PNG 同时校验区块边界、CRC、尺寸、IDAT 解压和解压后数据长度。
- 一次上传或创建任务前必须检查本批全部图片，并一次返回所有问题图片及原因。
- Codex 读取上下文和提交生成图时继续执行服务端兜底校验，阻止损坏图片进入生成或版本记录。

通过标准：

- 首次导入可选择 16:9 或 9:16，导入后画幅按钮不可用，顶部显示“已锁定”。
- 按新格式生成并导入的镜头，在批次选择窗口不再显示“未标注场景”。
- 上传一张正常图和多张损坏图时，正常图成功加入并高亮，损坏图在同一个错误清单中全部列出，页面不进入重命名。
- 旧项目中已经落盘的损坏参考图在创建运行前被整批拦截，Codex 不再到生图阶段才逐张报错。
- 自动测试、页面内联脚本检查、技能校验和插件校验全部通过；用户复验前状态保持“部分完成”。

### M1.2.1：参考图兼容与单次上限修复

目标：让审核台的资产选择与 Codex 内置生图的真实输入边界一致，并杜绝任务运行时静默丢弃无法读取的参考图。

范围：

- 每批最多选择 5 张参考图，资产区实时显示 `已选 N/5`；第 6 张在选择时即提示并阻止加入。
- 批次已满时继续上传，图片仍加入全局资产库，但不自动选入当前批次，避免用户重复上传。
- 创建 Codex 任务指令、复制首版/返修提示词和服务端创建运行时均再次校验上限，兼容并拦截旧数据中的超限批次。
- 浏览器把通过实际解码的新资产统一保存为 JPEG，最长边不超过 2048 像素、文件不超过 4 MB；服务端对尺寸和文件大小做同样的兜底校验。
- 插件必须逐张实际读取本批全部参考图；任一张在 Codex 侧无法解码时，一次汇总失败项、将运行标记为失败并停止，不得擅自删图或用可读子集继续生图。
- 内置生图只接收审核台返回且已确认可读的全部参考图，数量必须为 0–5 张。

通过标准：

- 隔离页面一次上传 6 张图后，全局资产为 6 张，当前批次只选 5 张，第 6 张有明确上限提示且不能选入。
- 曾触发 Codex 解码失败的两张场景图重新上传后，落盘为 2048×1151、远小于 4 MB 的 JPEG，并能被 Codex 图片读取能力逐张打开。
- 自动测试覆盖服务端超 5 张、超 4 MB 和超 2048 像素的整批拦截。
- 用户复验前状态保持“部分完成”；不因模型自行降级为更少参考图而记为成功。

### M1.3：第四轮验收修复

目标：压缩批次选择和 Codex 生图前后的非必要操作，并保证返修意见真实落到分镜脚本版本。

范围：

- 新建批次在中等及以上宽度使用双列镜头卡片；卡内将机位/构图、声音/情绪分栏，画面内容保持整行。
- 每次打开新建批次，默认从首个待分配镜头起连续选择，最多 9 镜；加入下一镜会使累计时长超过 15 秒时立即停止。
- 个人插件使用正式版号 `0.1.4` 直接安装，缓存目录、技能入口和插件清单都使用同一无后缀版本路径。
- 精简技能执行链路：读取批次、并行读取本批参考图、必要时确认返修脚本、调用一次内置生图、立即回填；删除环境扫描、插件路径查找、输出图片预览和生成后改写。
- 新增 `prepare_revision_script`：返修必须先提交完整新版分镜脚本；服务端拒绝镜号变化和内容未变化。
- 生图输入由审核台生成单一 `imageScript`；技能必须原样使用，不得附加审核意见、正/负向提示词、解释或生成后检查要求。
- 提交版本不再信任模型重复传回的脚本文本，直接使用该运行已确认的脚本，保证图文版本属于同一返修结果。
- 默认模型建议为 GPT-5.6 Luna + 低推理；脚本修改质量不足时再升级为 Terra + 低推理。

通过标准：

- 当前 Codex 内置浏览器可见双列卡片，默认勾选数和累计时长符合 9 镜 / 15 秒边界。
- 插件清单、缓存目录、技能文件与 MCP 服务均为无后缀 `0.1.4`。
- 自动测试证明未修改脚本不能进入返修生图，修改后的脚本会成为新版本的 `rawText`。
- 新 Codex 任务不再扫描技能安装路径或环境，不预览生成结果；除内置图片生成本身外只执行必要的上下文、脚本确认和回填调用。
- 一次真实返修验收中，新版脚本能看到意见对应字段变化，生图提示只等于 `imageScript`，总耗时接近内置图片生成时长。
- 用户完成真实耗时与效果复验前，本节点保持“部分完成”。

### M1.4：第五轮验收修复

目标：消除宫格声音占位噪声，降低跨批次漏选资产概率，并让分镜脚本结构支持场景分组和批内镜头合并/新增。

范围：

- 图片提示词明确规定底部只显示本镜实际存在的声音文案；缺失项完全省略，不显示字段标签，不补“无”，不输出空分隔符。
- 导入、导出、返修和生图脚本统一使用场景父级结构：连续同场景镜头只输出一次 `【场景：…】`；继续兼容旧版逐镜头 `场景：…` 和 `场景N：…` 输入。
- 新建批次默认沿用上一批已选的最多 5 张参考资产，在弹窗中显示可关闭的继承项和图片名称摘要；创建后仍可在资产区调整。
- 返修允许按审核意见在当前批次内合并或新增镜头；服务端按当前批次起始镜号连续编号，不信任模型自行编号。
- 当前批次通过时，以本批最终镜头数量替换原范围，并对后续待分配镜头统一平移镜号；顶部只显示待分配数量，不显示易过期的编号范围。
- 为保证已生成宫格的镜号不失配，MVP 只允许最新批次改变镜头数量；如果之后已有其他批次，先阻止通过并明确说明限制。
- 正式服务、个人插件和技能入口统一升级到无后缀 `0.1.5`。

通过标准：

- 自动测试覆盖声音清洗、场景父级往返、返修镜头新增和自动连续编号。
- 新建第二批时默认显示并勾选“沿用上一批参考资产”，创建后的资产选择与上一批一致且不超过 5 张。
- 使用含 `无 / 无 / 环境音`、`台词 / 无 / 无` 的脚本生成宫格，底部只出现实际声音内容，不出现“无/无/”或字段标签。
- 在最新批次返修中合并或新增镜头，通过后下一批待分配镜头从新的连续镜号开始。
- 正式健康接口、插件清单、缓存目录和技能入口均为 `0.1.5`。
- 用户完成真实生图和镜头重排复验前，本节点保持“部分完成”。

### M1.5：第六轮验收修复

目标：修复场景父级保存、批注引起的宫格定位漂移、镜头合并后的跨批编号错误和跨令牌旧图复用，并补齐可导出的运行日志。

范围：

- 场景组标题改为普通文档流，不再使用悬浮吸顶，避免遮挡镜头内容。
- 修复旧 `rawBlock` 升级时覆盖父级场景的问题；标准场景组导入并保存后仍保留场景名称。
- 镜头旁批注继续汇总到底部；汇总区使用固定高度滚动容器，宫格覆盖层通过尺寸监听自动重新对齐。
- 已通过批次以 `rangeStart + 组内顺序` 为权威镜号，统一修复版本偏移、总池缺口和相邻批次重号。
- 运行创建时冻结镜头脚本、镜号、画幅和资产选择，后续令牌读取不受页面上其他批次变化影响。
- 回填图片记录 SHA-256；与任一历史版本内容完全相同的图片不允许作为新运行结果，并要求重新调用内置生图。
- 记录任务创建、上下文读取、运行状态、返修脚本准备、版本回填和失败事件；页面提供“导出排查日志”。
- 正式服务、个人插件和技能入口统一升级到无后缀 `0.1.6`。

通过标准：

- 用户提供的场景父级脚本导入后不出现“未标注场景”，滚动时场景标题不遮挡镜头卡片。
- 新增和删除定位批注时，底部仍能完整汇总，宫格框选位置始终与图片格子一致。
- 前一批从 9 镜合并为 8 镜并通过后，下一批默认从新起点连续选择 9 镜，之后批次不重复上一批末镜。
- 不同令牌提交相同历史图片内容时服务端拒绝；实际调用一次新生图后才能回填。
- 可从页面导出最近运行日志，日志不包含完整令牌或完整提示词。
- 自动测试、页面脚本、技能和插件校验通过；真实页面和新任务复验完成前保持“部分完成”。

### M1.6：第七轮验收修复

目标：取消跨批镜头增减阻断，建立“已创建批次冻结、下一批衔接、终版统一校号”的编号规则，并补齐最新批次删除和导入锁定。

范围：

- 已创建批次保留自身镜号；其他批次增减镜头时不重排、不阻止通过。
- 新建批次前，仅把待分配镜头从最新批次最后一镜之后连续编号；批次创建时保存源镜头快照，之后不再依赖总池镜号定位。
- 终版导出按批次创建顺序检查全量镜号；发现缺号或重复时，对导出副本从 1 连续重编号，不修改历史批次和宫格图。
- 只允许删除最新批次；服务端再次校验目标。已有版本时二次确认，并删除该批版本图片、返修记录和运行记录。
- 项目已有脚本或批次时禁用“导入分镜脚本”，提示先清空当前项目。
- “导出排查日志”统一改名为“导出操作记录”。
- 正式服务、个人插件和技能入口统一升级到无后缀 `0.1.7`。

通过标准：

- 旧批增减镜头后可正常通过，已经创建的后续批次镜号不变化。
- 再创建下一批时，待分配首镜等于当前最新批次末镜加 1，且批次内连续、不重复。
- 构造跨批缺号或重复数据后导出，终版脚本镜号为连续 `1..N`，页面历史批次保持原状。
- 非最新批次删除请求被拒绝；删除最新批次前按是否已有版本显示对应确认文案，确认后镜头退回待分配。
- 已导入项目的导入按钮不可用并说明清空要求；空项目恢复可用。
- 自动测试、页面脚本、技能和插件校验通过；页面复验完成前保持“部分完成”。

### M1.6.1：镜号一致性热修复

目标：修复待分配镜头重排后，页面脚本、Codex 生图脚本、回填版本和宫格点击热区仍使用不同镜号的问题。

范围：

- 批次源镜头、运行上下文和回填版本统一按 `rangeStart + 组内顺序` 编号。
- 读取旧运行上下文时也按批次起点即时规范镜号，避免旧源编号继续进入图片模型。
- 宫格点击热区使用当前页面展示镜头的顺序与编号，不直接读取可能尚未规范的版本原值。
- 正式服务、个人插件和技能入口统一升级到无后缀 `0.1.8`。

通过标准：

- 构造批次起点为9、源镜头仍为10–18的数据，运行上下文和生图脚本必须为9–17。
- 回填版本脚本仍为9–17；宫格九个点击热区依次定位右侧#9–#17。
- 自动测试、页面脚本、技能和插件校验通过；真实宫格重新生成复验前保持“部分完成”。

### M1.7.2：学员安装包可靠性热修复

目标：从发布包根源修复干净 Windows 电脑双击安装闪退和 Codex 插件刷新无法解析 `CODEX_HOME` 的问题。

范围：

- Windows 发布包中的 `一键安装.cmd`、`一键启动.cmd` 强制使用 CRLF 换行。
- 安装入口无论成功或失败都保留窗口，失败时引导读取 `install-log.txt`。
- 安装器创建并显式传入当前用户的 `CODEX_HOME`，同时兼容新版 Codex Windows 命令位置。
- 新增通过真实 `cmd.exe`、隔离用户目录和模拟 Codex CLI 执行的端到端安装测试。
- 发布独立 `v0.1.9`，避免学员继续拿到浏览器或网盘缓存的 `v0.1.8` 坏包。

通过标准：

- 发布包两个 `.cmd` 文件不存在孤立 LF。
- 不预设 `CODEX_HOME` 时，一键安装能够完成，并保证每次 Codex 插件命令收到同一个已创建目录。
- 安装失败时窗口保留且生成日志；完整自动测试、插件校验、隔离安装和便携启动全部通过。
- GitHub `v0.1.9` Release 可下载，SHA256 与本地最终包一致。

### M1.7.3：ChatGPT/Codex Windows App 识别热修复

目标：修复干净电脑已安装并打开 ChatGPT/Codex Windows App，但没有 PATH、独立 CLI 或 `manual-cli` 时，一键安装误报“未找到 Codex”的问题。

范围：

- 优先识别 `%USERPROFILE%\.codex\plugins\.plugin-appserver\codex.exe`。
- 在上述组件尚未生成时，通过已安装的 Windows 应用包定位 App 内置 Codex 组件，并复制到工具包内部后执行插件安装。
- 保持工具包可从任意本地目录安装，不要求放进 ChatGPT/Codex 安装目录；移动工具包后需重新安装以刷新绝对路径。
- 新增完全无 PATH、无独立 CLI、无 `manual-cli`、无 `plugin-appserver` 的隔离安装回归。
- 发布独立 `v0.1.10`，停止分发会误判桌面 App 的 `v0.1.9`。

通过标准：

- 只有 Windows App 内置组件可用时，一键安装仍返回成功并完成插件 remove/add/list。
- 从含中文、空格的任意非 Codex 目录运行不影响安装和启动。
- 常规测试、真实 `cmd.exe` 安装测试、插件校验、最终发布包安装与健康检查全部通过。
- GitHub `v0.1.10` Release 可下载，SHA256 与本地最终包一致。

### M1.7.4：macOS 通用学员安装包

目标：为不使用 GitHub 和命令行的 Mac 学员提供与 Windows 版一致的一键安装、一键启动和 Codex 个人插件体验。

范围：

- 单个 ZIP 同时内置 Apple Silicon `arm64` 与 Intel `x64` Node.js 运行环境，并按处理器自动选择。
- 提供 `一键安装.command` 与 `一键启动.command`，ZIP 保留 Unix 可执行权限。
- 安装器兼容 macOS 的 `~/.codex`、个人插件目录和个人市场；优先复用本机 Codex，缺失时调用 OpenAI 官方独立安装器。
- 重复启动时只打开现有页面，不产生端口占用堆栈。
- 提供面向小白的 macOS 使用说明、Gatekeeper 右键打开指引、安装日志和测试用例。

通过标准：

- 包结构检查确认双架构 Mach-O 二进制、可执行权限、无用户 `data/` 和无开发机路径。
- Windows 既有安装与业务自动测试不回归。
- Mac 真机完成安装、重启 Codex、启动审核台、创建任务、读取批次和重复启动冒烟。
- 未收到用户明确发布指令前，只交付本地安装包，不上传 GitHub。

### M1.9：不中断输入与品牌标识（本地待发布）

目标：后台任务完成时不打断当前批注输入，并统一审核台与 Codex 插件的品牌识别。

范围：

- 后台轮询触发工作区刷新前保存当前审核意见、光标选区和输入框滚动位置，刷新后无感恢复。
- 以用户提供的小人 IP 为主体，设计“小人骑木马＋四格分镜”共用图标。
- 审核台左上角、浏览器页签和 Codex 插件使用同一图标；页面与插件清单标注开发者“YIQI玩AI”。
- 先更新本地源码、正式验收服务和当前电脑个人插件，不生成新安装包、不推送 GitHub；随下一次正式版本统一发布。

通过标准：

- 其他批次回填完成并出现 Toast 时，当前审核批注输入框不失焦，文字、光标与滚动位置不变。
- 审核台和插件图标清晰可辨，透明边缘正常，开发者名称准确且不挤占主要操作区域。
- 自动测试、插件清单校验和技能校验全部通过。

### M2：审核台一键交给 Codex

目标：去掉手动新建任务和粘贴指令的步骤。

范围：

- 使用 Codex App Server 的 `stdio` JSONL 接口。
- 首次生成使用 `thread/start` 创建批次专属任务。
- 同批返修使用 `thread/resume`；不同批次不得复用任务 ID。
- 使用 `turn/start` 发送批次指令和本次资产。
- 处理运行、完成、失败、审批、取消和重试状态。
- 结果仍通过 `submit_storyboard_version` 回填，不扫描聊天输出猜测结果。

开始条件：

- M1 已通过并稳定保留人工任务交接能力。
- 已验证 App Server 创建的任务能加载个人插件和 MCP 工具。
- 已验证 App Server 任务可以调用内置生图。
- 已明确任务在 Codex 桌面端的可见性和后续继续操作方式。

通过标准：

- 页面一次点击即可启动本批 Codex 任务。
- 同批返修复用任务，不同批次任务 ID 不同。
- 页面能取消运行并在失败后重试。
- App Server 不可用时可无损退回 M1 的人工交接流程。

### M3：体验和数据可靠性完善

范围：

- 状态轮询升级为 SSE 实时事件。
- 增加运行日志抽屉、取消和失败重试入口。
- 增加自包含项目备份与恢复。
- 增加版本来源、返修依据和反馈去向的清晰展示。
- 完善宫格布局校验、窄屏显示和错误提示。
- 评估 WebMCP；只有确实能减少操作步骤时才加入。

通过标准：

- 备份恢复后图片、版本、批注和批次状态一致。
- 400px、600px 和桌面宽度下核心流程可操作。
- 网络或 Codex 进程中断不会损坏已有项目。

### M4：插件产品化与发布

范围：

1. 完成个人市场插件的安装、更新和新任务验证。
2. 补齐版本、说明、权限、隐私、故障诊断和兼容性信息。
3. 根据届时的官方要求准备公共市场提交材料。

通过标准：

- 新安装环境可以按文档完成本地服务与插件安装。
- 插件只申请完成工作流必需的最小权限。
- 插件校验工具和 MCP 检查全部通过。
- 公共发布若受平台审核或政策限制，明确记录为外部依赖，不虚报完成。

## 5. 批次隔离规则

隔离同时在任务、工具和提示词三层实施：

1. 一个批次对应一个 Codex 任务。
2. 一个生成请求对应一个随机运行令牌。
3. MCP 工具根据令牌只返回对应批次的数据。
4. 只返回当前选中的人物、场景和道具资产。
5. 默认不返回历史宫格图片路径。
6. 不同批次不得共享 `codexThreadId`。
7. 同批返修可以复用任务，但不得主动把旧宫格作为参考图再次上传。

固定生图约束：

> 调用 Codex 内置图片生成能力完成本次生图。仅参考本次提供的人物资产图、场景资产图、道具资产图和本次提示词文字。不得参考、复用、临摹或延续本任务中此前生成的任何分镜宫格图；此前宫格图仅代表待修结果，不属于本次参考输入。每个画格严格使用所选 `16:9` 或 `9:16` 比例，左上角只标 `#镜号`；底部只显示本镜实际存在的声音文案，缺失项不补“无”且不输出空分隔符。完成后必须调用分镜审核台的提交版本工具回填结果。

限制说明：同批返修如果复用同一个任务，旧图仍可能存在于任务历史中，因此无法得到数学意义上的完全隔离。如果实测影响明显，再将策略升级为“每轮返修创建新任务”。

## 6. 第一版接口边界

计划中的本地 HTTP 接口：

- `GET /api/bootstrap`
- `PUT /api/project`
- `POST /api/assets`
- `GET /api/assets/:id/file`
- `POST /api/batches/:id/runs`
- `GET /api/runs/:id`
- `POST /api/runs/:id/status`
- `POST /api/runs/:id/versions`
- `POST /api/migrations/local-storage`

核心数据实体：

- `Project`：项目名称、镜头池、活动批次和界面偏好。
- `Asset`：名称、人物/场景/道具类型、文件路径和创建时间。
- `Batch`：镜头范围、资产选择、状态、批注和版本列表。
- `Run`：批次、令牌、运行状态、失败信息和幂等键。
- `Version`：图片、脚本文本、镜头、宫格布局、来源运行和创建时间。

## 7. 落库与验收制度

- 本计划文件是范围基线；范围变化必须先更新计划再实现。
- 每个里程碑通过后，在 `里程碑记录/` 新建一份记录，不覆盖历史记录。
- 文件名格式：`M编号-YYYYMMDD-简短名称.md`。
- 记录必须包含：交付物、验收项、自动测试、人工验证、已知限制、遗留风险和下一里程碑。
- 每条验收项必须附证据，例如测试命令与结果、状态文件、截图路径或实际回填版本编号。
- 只有全部阻断级验收项通过，才能把状态写为“已通过”。
- 未通过时可以记录“阻塞”或“部分完成”，但不得提前记为完成。
- 如果后续回归发现已通过节点失效，新建回归记录，不改写原记录。
- 每次迭代不能以“代码已修改”作为完成条件；交付前必须完成正式验收服务更新、个人插件同步与重载、README、测试用例和里程碑记录更新。
- 正式验收地址必须实际启动并通过健康检查；若版本号发生变化，健康接口、页面、插件和文档中的版本信息必须一致。
- 最终回复必须提供可直接打开的正式验收地址、测试用例和里程碑记录；涉及插件更新时同时提供个人插件的查看与分享链接。
- 不把临时端口、隔离测试数据或仅开发环境可访问的页面交给用户验收。

### 每次迭代的发布检查清单

1. 自动测试、关键页面流程和数据兼容性验证通过。
2. 正式服务重启到本次版本，并确认用户现有项目数据未丢失。
3. 个人插件源码同步、缓存版本更新、校验并重新安装启用。
4. 更新 `README.md`、`测试用例.md`、本计划及对应里程碑记录。
5. 生成最新 Windows 安装包，打开并保留正式验收页面，最终回复给出可直接验收的链接和最短验收步骤。
6. 日常迭代不自动上传 GitHub；只有全部流程验收通过且用户明确下达“发布到 GitHub”指令后，才统一同步源码并创建公开 Release。

## 8. 当前执行顺序

1. 落库本计划和里程碑记录模板。
2. 执行 M0 最小技术验证。
3. M0 通过后写入里程碑记录。
4. 实现 M1 本地服务、前端接线和个人插件。
5. 完成自动测试与真实双向回填验收。
6. 根据用户验收反馈完成 M1.1 并追加里程碑记录。
7. 根据第二轮验收反馈完成 M1.2，并追加测试用例与里程碑记录。
8. 完成 M1.2.1 参考图兼容与单次 5 张上限修复，更新个人插件和正式验收服务。
9. 完成 M1.3 第四轮验收修复，正式服务与个人插件升级到无后缀 `0.1.4`。
10. 完成 M1.4 第五轮验收修复，正式服务与个人插件升级到无后缀 `0.1.5`。
11. 完成 M1.5 第六轮验收修复，正式服务与个人插件升级到无后缀 `0.1.6`。
12. 完成 M1.6 第七轮验收修复，正式服务与个人插件升级到无后缀 `0.1.7`。
13. 完成 M1.6.1 镜号一致性热修复，正式服务与个人插件升级到无后缀 `0.1.8`。
14. 用户完成一次批次冻结、下一批衔接、镜号一致性、终版校号、批次删除和导入锁定复验后追加通过记录，再决定是否启动 M2。
15. 根据交付决定冻结 `0.1.8` 为首个学员使用版，完成产品化 README、真实演示素材、免开发环境 Windows 工具包、一键安装/启动、个人插件刷新和干净目录验收；后续功能反馈进入下一版本。
16. 完成 M1.7.1 学员 Codex 任务入口说明热修订：统一明确“工作、无项目、本地”，同步 README、使用说明、审核台入口提示和 Windows 发布包，不改变 `0.1.8` 功能版本与插件实现。
17. 完成 M1.7.2 学员安装包可靠性热修复：统一升级到 `0.1.9`，修复 Windows 批处理换行和显式 `CODEX_HOME`，新增真实 `cmd.exe` 隔离安装回归并发布全新下载包。
18. 完成 M1.7.3 Windows App 识别热修复：统一升级到 `0.1.10`，支持无 PATH、无独立 CLI、无 `manual-cli` 和无 `plugin-appserver` 时从已安装的 ChatGPT/Codex Windows App 获取安装组件，并明确工具包可位于任意本地目录。
19. 执行 M1.7.4 macOS 通用包：先完成双架构本地构建和静态/结构验证，记录为“待 Mac 真机复验”；真机通过后再把里程碑改为已通过。
20. 执行 M1.8 分类资产索引板：每批参考资产上限调整为 14；1–5 张直接使用原图，6–14 张固定生成 5 张单类型 2×2 索引板，每板最多 4 项；生图提示词同步加入索引编号、类型和名称映射。全部测试通过后同时更新 Windows、macOS 安装包、个人插件和 GitHub Release。
21. 执行 M1.9 不中断输入与品牌标识：修复后台回填重绘导致批注输入框失焦的问题，统一审核台和个人插件图标及“YIQI玩AI”开发者标识；仅保存在本地，随下次正式版本统一生成双平台安装包并推送 GitHub。
22. 执行 M1.10 双平台整合包：版本统一升级到无后缀 `0.1.12`，把 M1.8 与 M1.9 改动合并进 Windows、macOS 安装包；补充旧版数据迁移和插件覆盖升级说明，并在旧版仍占用端口时阻止误开旧页面。本节点只生成本地交付包，等待用户明确指令后再推 GitHub。
