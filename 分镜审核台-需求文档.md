# 分镜审核台 · 现状产品需求

> 阶段稳定基线：`v0.1.12`
>
> 现状冻结日期：2026-09-15
> 本文只描述已经交付的能力；M2.1 及以后规划以《迭代计划》为准。

## 1. 产品定位

分镜审核台是面向 AI 短剧创作者的本地生产工具。它把结构化分镜脚本、人物/场景/道具参考资产、Codex 内置图片生成、宫格审核、脚本返修和终版导出串成一个可追溯闭环。

核心价值：

- 一个批次对应一个独立 Codex 任务，降低跨批次参考污染。
- 宫格图与分镜脚本同屏审核，批注集中汇总。
- Codex 通过个人插件读取冻结上下文，并把新图和脚本版本自动回填。
- 项目、图片和操作记录默认只保存在用户本机。

## 2. 目标用户与运行环境

- 目标用户：使用 ChatGPT/Codex 桌面端制作 AI 短剧分镜的创作者和学员。
- 本地系统：64 位 Windows 10/11；Apple Silicon 或 Intel macOS。
- 页面入口：本地服务 `http://127.0.0.1:43127/`，可在系统浏览器或 Codex 浏览器面板打开。
- Agent 入口：Codex“工作、无项目、本地”任务；每个新批次新建任务，同批返修可继续原任务。
- 模型建议：GPT-5.6 Luna、低推理；脚本修改质量不足时再升级模型。
- 用户不需要安装 Git、GitHub、Node.js 或 Python；安装包自带运行环境。

## 3. 当前范围与非目标

### 3.1 当前范围

- 单用户、单机、单个活动项目。
- 结构化分镜脚本导入、解析和文本导出。
- 批次创建、冻结、删除最新批次和通过。
- 人物、场景、道具三类参考资产。
- 每批最多 14 项资产；超过 5 项时整理为 5 张分类索引板。
- Codex 个人插件、随机运行令牌和 MCP 自动回填。
- 首版生成、脚本返修、多版本审核和操作记录导出。
- Windows/macOS 便携安装、覆盖升级和项目数据迁移。

### 3.2 当前非目标

- 不提供账号、多用户协作、云同步或教师审核。
- 不由平台统一承担图片模型费用，不要求用户填写 OpenAI API Key。
- 不从网页自动创建 Codex 任务；当前由用户复制指令并新建任务。
- 不向公网自动上传剧本、批注、图片、提示词或本地路径。
- 不承诺公共插件目录已经上架；GitHub 发布与公共目录审核是不同状态。

## 4. 主工作流

```text
导入结构化分镜脚本并确认画幅
  → 自动打开首批创建窗口
  → 选择最多 9 镜且累计不超过 15 秒
  → 选择本批人物/场景/道具参考资产
  → 审核台校验资产并创建随机运行令牌
  → 用户在 Codex 新建独立本地工作任务并粘贴指令
  → 插件只读取本批冻结脚本、画幅和资产
  → Codex 调用内置图片生成能力
  → 插件回填新图片与对应分镜版本
  → 用户审核、批注、返修或通过
  → 创建下一批，最终导出连续编号的终版分镜脚本
```

## 5. 功能需求

### 5.1 分镜脚本导入与解析

- 导入时先选择 `16:9` 或 `9:16`，默认 `16:9`；导入后画幅锁定，清空项目后才能重新选择。
- 支持标准逐镜文本、JSON 数组和竖线分隔格式。
- 标准字段包括：场景父级、镜号、时长、机位/景别/运镜、构图、画面内容、台词/心声/音效、情绪目标。
- 连续同场景镜头使用上一级 `【场景：…】`；继续兼容旧版逐镜头场景字段。
- 项目已有脚本或批次时禁用再次导入，并提示先清空项目。
- 导入完成后自动打开“新建批次”，引导创建第一批。

### 5.2 批次管理

- 新建批次只选择待分配镜头，默认从最前面连续选择。
- 默认选择上限为 9 镜；加入下一镜会使累计时长超过 15 秒时停止选择。
- 批次创建时冻结源镜头快照、起始镜号、画幅和资产选择，后续总池变化不改写已创建批次。
- 新批次从最新批次最后一镜之后重新连续编号。
- 当前批次返修允许在批内合并或新增镜头，最多 20 镜；服务端按批次起点重新连续编号。
- 只允许删除最新批次；有生成版本时必须二次确认，并同步删除该批版本图片和运行记录。
- 批次通过后不再允许创建新运行。

### 5.3 参考资产

- 全局资产分为人物、场景、道具三类；用户可重命名并切换类型。
- 新上传资产短暂高亮并默认勾选到当前批次，不自动进入重命名状态。
- 下一批默认沿用上一批资产选择，用户可以在创建批次和资产区调整。
- 浏览器上传时实际解码图片并转为兼容 JPEG；最长边不超过 2048px、文件不超过 4MB。
- 服务端再次验证文件结构、尺寸、大小和路径；一次返回本批全部问题，不让 Codex 逐张报错。
- 每批最多选择 14 项资产：
  - 0～5 项：Codex 直接接收原图。
  - 6～14 项：审核台固定生成 5 张索引板，尽量使用满 5 张输入额度。
  - 每张索引板只含人物、场景或道具中的一种类型，每板最多 4 项，以 A01、A02…标识。
  - 生图脚本包含编号、类型和名称映射；Codex 只接收索引板，不再接收源图。

### 5.4 Codex 任务与批次隔离

- 创建任务时生成不可预测的运行令牌；令牌只对应一个批次和一次运行。
- 运行冻结本批脚本、镜号、画幅、资产 ID、索引板和返修意见。
- 插件只返回运行令牌对应的数据，不返回其他批次资产或历史宫格路径。
- 同批同时只允许一个进行中的任务；失败后可以重新创建运行。
- 不同批次必须新建 Codex 任务；同批返修可以留在原任务中。
- 运行状态包括 ready、preparing、generating、submitting、completed、failed。

### 5.5 首版生成与返修

- 首版生图提示由审核台生成唯一 `imageScript`，包含宫格布局、画幅、声音文字规则、资产映射和完整本批脚本。
- Codex 必须把 `imageScript` 原样交给内置图片生成能力，不得添加正向/负向提示、审核意见、解释或生成后检查要求。
- 返修必须先把审核意见落实为一份可解析的新分镜脚本，并调用 `prepare_revision_script`。
- 未修改脚本、脚本无法解析或镜头超过 20 个时停止返修生图。
- 返修生图只能使用审核台确认后的新版脚本，不把审核意见作为额外生图提示。
- 每次运行必须产生新图片；与任一历史版本内容完全相同的图片不得作为新结果提交。

### 5.6 宫格与审核

- 所有镜头合并在一张宫格图中，按镜号从左到右、从上到下排列。
- 单格使用项目锁定的 `16:9` 或 `9:16` 画幅。
- 每格左上角只显示 `#镜号`。
- 底部只显示本镜实际存在的声音文案；不显示字段标签、“无”、`无/无/` 或空分隔符。
- 点击宫格格子定位右侧分镜；点击脚本卡片也可反向定位宫格。
- 宫格覆盖层跟随图片渲染尺寸重算，批注区域高度变化不应造成定位偏移。
- 镜头旁批注统一汇总到底部固定高度审核批注区域，避免散落和输入区跳动。
- 后台任务完成 Toast 不得抢走审核批注输入焦点，并保留草稿、光标选区和滚动位置。

### 5.7 版本、通过与导出

- 每次成功回填创建不可变版本，保存宫格图、分镜文本、镜头、反馈、行列、来源运行和创建时间。
- 同一幂等键重复提交只返回已有版本，不创建重复版本。
- 用户手动决定是否通过批次；插件不能自动通过、清空项目或删除历史版本。
- 终版导出按批次创建顺序汇总；出现缺号或重复时，仅对导出副本从 1 连续重编号，不修改历史宫格和批次。
- 支持导出项目备份、恢复备份、清空项目和导出操作记录。

### 5.8 品牌与界面

- 顶栏、favicon、空状态和 Codex 插件使用统一“小人骑木马＋四格分镜”图标。
- 顶栏显示 `by YIQI玩AI`，空状态显示 `YIQI玩AI · 出品`。
- 审核工作区不重复增加品牌元素，不影响信息密度和操作效率。
- 所有 Toast 位于页面上方；导入成功等关键节点提供明确下一步引导。

## 6. 本地服务与数据要求

- Node.js 本地服务只监听 `127.0.0.1:43127`。
- 元数据保存在 `data/project.json`；图片分别保存在 `data/assets/`、`data/reference-sheets/` 和 `data/versions/`。
- 操作记录保存在 `data/logs/storyboard-review.jsonl`；备份保存在 `data/backups/`。
- 写请求串行执行；项目状态先写临时文件，再原子替换正式文件。
- 页面保存使用 revision 乐观并发控制，冲突时重新读取，不静默覆盖。
- 旧 localStorage 数据只在空项目中迁移一次，原数据保留作为回退。

## 7. MCP 插件要求

正式插件版本与产品版本一致，`v0.1.12` 提供五个工具：

- `get_batch_context`：读取令牌对应的冻结批次上下文。
- `get_batch_status`：查询运行和回填状态。
- `report_generation_status`：记录生成阶段或失败原因。
- `prepare_revision_script`：确认返修后的完整分镜脚本并生成唯一生图脚本。
- `submit_storyboard_version`：提交本次新图，创建版本并自动回填。

插件不得提供自动通过、清空项目、删除批次或覆盖历史版本的能力。

## 8. 安装、升级与交付

- Windows 包包含 `一键安装.cmd` 和 `一键启动.cmd`。
- macOS 通用包包含 `一键安装.command` 和 `一键启动.command`，同时携带 arm64 和 x64 运行环境。
- 安装器部署个人插件、登记个人市场并刷新 Codex 插件缓存；发布 ZIP 本身不会自动更新其他电脑。
- 升级时关闭旧服务和 Codex，把旧包 `data/` 复制到新包，运行新版一键安装，重启 Codex，并新建本地工作任务。
- 工具包可位于任意本地目录，不需要放进 Codex 安装目录；安装后移动目录必须重新安装。
- macOS 包尚未使用 Apple Developer 证书签名，首次打开需要右键或按住 Control 选择“打开”。

## 9. 隐私与安全边界

- `v0.1.12` 没有账号、匿名埋点、云同步或业务后端。
- 项目内容默认只写入用户本机工具包目录。
- 审核台只把当前批次勾选的资产和文本交给用户主动创建的 Codex 任务。
- 导出的备份和操作记录可能包含创作信息，用户应自行决定分享范围。
- 未来观测和数据共创必须遵守独立授权、最小字段、可关闭和可撤回原则；本节点不实现上传。

## 10. 已知限制

- 创建 Codex 任务仍需复制指令并由用户手动新建任务。
- 同批返修复用原任务时，历史图片仍存在于任务历史；审核台和技能会禁止主动再次引用，但无法提供数学意义的上下文清除。
- 宫格内中文文字由图片模型生成，可能出现字形错误，需要返修。
- 总耗时包含 Codex 排队、任务启动和图片生成，审核台只能精简自身调用链，不能保证固定秒数。
- macOS Gatekeeper 与个人插件安装仍需在真实 Mac 上持续验证。
- 页面样式仍通过 Tailwind 公网 CDN 加载；干净电脑首次完全离线打开时，核心服务与本地数据可用，但页面样式可能不完整。
- `v0.1.12` 的干净电脑升级矩阵尚未全部取得真实机器证据，详见双平台验收矩阵。

## 11. 阶段验收口径

- 自动测试、插件校验、正式健康接口和 Release 文件校验通过。
- Windows 与 macOS 的安装、升级、启动、插件识别、生成、返修、回填、导出和重复启动分别取得真机证据。
- 源码提交、Git 标签、安装包 SHA256、插件版本和公开 Release 能相互追溯。
- 未取得的真机证据必须标为待验收或阻塞，不以静态检查替代。
