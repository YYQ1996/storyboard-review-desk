# 分镜审核台 · 需求文档

> AI 短剧分镜生产审核工具 · 单文件 HTML
> 版本：v1.0 ｜ 更新日期：2026-09-08

---

## 一、项目背景与目标

AI 短剧生产流程中，分镜宫格图由 Codex（AI 编程/生图助手）生成，需要在「生图 → 审核 → 返修」之间反复迭代。本工具嵌入 Codex 右侧浏览器面板使用，目标是把整个审核闭环收敛到一个页面内：

1. 导入分镜脚本，按连续镜头手动划分批次；
2. 为每个批次生成「首版生图提示词」，复制给 Codex 生成**一张多宫格图**（所有镜头合在一张图里，每格左上角带镜头号，节省生图开支与耗时）；
3. 回填 Codex 结果（脚本文本 + 宫格图），形成版本；
4. 图、文同屏对照审核，支持选中文字在位添加注释，注释自动汇入审核批注框；
5. 一键复制「返修提示词」（严格约束：只按批注修改、不自由发挥、不增删镜头、不调换顺序）；
6. 反复迭代直至通过，批次锁定，镜头总池同步，自动开启下一批；
7. 全部批次通过后，按指定文本格式导出终版分镜脚本。

## 二、使用环境

- 嵌入 Codex 右侧浏览器面板（宽度较窄，约 400–600px），同时兼容手机 / 平板 / 桌面；
- 纯前端运行，无后端；数据保存在浏览器 localStorage（key：`storyboard_review_v1`），支持导出 / 恢复 JSON 备份；
- 宫格图与脚本的对照为**左右同屏布局**（任何宽度下都不退化为上下结构），中间分隔条可拖拽调比例（22%–78%），比例持久化。

## 三、技术约束

| 项 | 要求 |
| --- | --- |
| 结构 | 单个 HTML 文件，完整可直接运行 |
| 样式 | Tailwind CSS v3（CDN 引入），禁止其他第三方 CDN / 本地依赖 |
| 脚本 | 原生 JS，写在页面内 `<script>` 标签中，带基础注释 |
| 语义化 | 使用语义化 HTML 标签 |
| 视觉 | 现代简洁的暗色工作台风格，hover / 过渡动效完整 |

## 四、核心工作流

```
导入脚本 → 待分配池 → 新建批次（勾选连续镜头，场景切换自动提示拆分）
  → 复制首版生图提示词 → 粘贴到 Codex 生成一张多宫格图
  → 回填（粘贴脚本 + 上传/拖入宫格图）→ 生成新版本（审核中）
  → 图文同屏对照、选中文字加注释 → 复制返修提示词 → Codex 重出图
  → 再次回填（版本递增 v2、v3…）→ 直至「通过本批」
  → 批次锁定、镜头池同步（审核中增删的镜头以最终版为准）
  → 自动开启下一批 → 全部通过后导出终版脚本（.txt）
```

## 五、功能需求明细

### 5.1 脚本导入与解析

- 支持粘贴文本导入，解析器依次尝试三种格式：
  1. **标准分镜格式**（用户实际使用格式，导入导出必须无损往返）：
     ```
     镜头10
     机位/景别/运镜：沈昭宁面部大特写
     画面内容：……
     台词 / 音效：……
     ```
  2. JSON 数组；
  3. 竖线分隔行（镜号|机位|场景|时长|画面|台词）。
- 解析字段：镜号、机位/景别/运镜、场景、时长、画面内容、台词/音效；
- 未分配镜头显示在顶部「待分配」胶囊中（如：待分配 5 个镜头 #10–#14）。

### 5.2 批次管理

- 手动勾选连续镜头建批；「智能建议」按场景自动圈出下一个连续场景的镜头（脚本无场景字段时给出提示，不会全选）；
- 场景切换时提示是否拆分批次；
- 批次状态机：`待生图 → 审核中 → 已通过`；通过后锁定（资产勾选、注释等不可再改）；
- **新建批次后、首版宫格图生成前，脚本区立即从镜头池预填本批镜头内容**，并提供「复制首版生图提示词」；
- 批次通过时把该批最终版镜头同步回总池（审核期间增删的镜头以最终版为准），随后自动弹出下一批的新建窗口。

### 5.3 提示词生成（复制给 Codex）

- **首版提示词**：批次镜头全文 + 参考资产 + 宫格布局要求（"宫格严格按 C 列 × R 行排列"）+ 每格左上角标注镜头号 + 输出为一张图；
- **返修提示词**：上一版脚本全文 + 审核批注 + 严格约束（只按批注修改、不增删镜头、不调顺序、保持脚本原有字段与文本格式、不要改成 JSON）；
- 提示词中的参考资产按类型分组列出（人物图：xx；场景图：yy），并要求人物形象 / 场景环境与参考资产严格一致。

### 5.4 参考资产

- 资产为**全集共用**，每批通过勾选决定本次生图参考哪几张；
- 资产分**人物图 / 场景图**两组：
  - 卡片左下角「人/场」标签点击切换类型；
  - 标题行「新图归为［人物｜场景］」开关决定新上传 / 拖入图片的默认分组；
- **重命名**：点击资产名称原地编辑（回车 / 失焦保存，Esc 取消），用于与剧本角色名、场景名对齐；新图上传后自动进入重命名态；
- 支持本地文件上传、从 Codex 页面直接拖入图片（跨域失败时给出提示）；
- 展示策略：收起态单行平铺（人物、场景两组同排可见，超出截断）；「展开全部」切换为分组网格 + 纵向滚动（最高 38vh）；
- 组内排序：本批已勾选的自动排在最前，未勾选在后；
- 头部信息行：标题 + ⓘ 悬停提示（操作说明收纳其中，不占行）+ 本批已选数量 + 展开/收起。

### 5.5 宫格图查看器

- 宫格图是**一整张图**（非多卡片拼排），支持点击放大（灯箱）；
- 图上覆盖透明点击热区：点击某格 → 右侧对应镜头卡片进入**持久选中态**（琥珀色边框，非闪烁动画），并平滑滚动到可视区；反向点击脚本卡片的镜号也可定位到格子；
- 热区行列**不写死**：默认按镜头数估算（⌈√n⌉ 列），每个版本可在查看器头部用「列 / 行」选择器手动指定（1–5 列 × 1–4 行），回填时也可指定；布局同时写入提示词，保证 Codex 出图排列与热区一致；
- 热区通过读取图片实际渲染位置精确对齐，窗口缩放、拖拽分栏时自动重算。

### 5.6 审核与注释

- 左图右文同屏对照；审核中状态下，在右侧脚本文字上**划选问题文字**，弹出在位注释浮窗（自动带上镜头号和原文引用），填写后自动汇入底部「审核批注」框；
- 批注格式：`【镜头N】原文「……」存在问题：……`，可删除；底部另有整体意见补充框；
- 点击「复制返修提示词」时，注释 + 整体意见一并组入；
- 回填新版本后，上一轮注释清空（已随提示词发出），开始新一轮。

### 5.7 版本管理

- 每次回填生成一个版本（v1、v2…），保留：宫格图、脚本镜头、原始文本、反馈、创建时间、宫格行列；
- 查看器头部可切换查看历史版本；
- 批次 Tab 上显示状态点（待生图·蓝 / 审核中·橙 / 已通过·绿）。

### 5.8 导出

- **导出终版脚本**（.txt，非 JSON）：汇总全部批次（按批内最小镜号排序），每批取最新版本镜头（未生图批次取池内镜头），按镜号排序，严格输出标准分镜格式：
  ```
  镜头1
  机位/景别/运镜：……
  画面内容：……
  台词 / 音效：……
  ```
- 导出备份（JSON）/ 恢复备份 / 清空重置（二次确认）。

## 六、关键交互决策记录

| 决策 | 原因 |
| --- | --- |
| 回填为半自动（复制提示词 → Codex 生成 → 手动回填） | 纯前端无法接收 Codex 回推，回填弹窗是最小成本的闭环 |
| 图文必须同屏左右对照，且任何宽度不堆叠 | 审核时先看图、按镜头号找脚本、逐字对照，上下结构距离过远不可用 |
| 格子点击为持久选中态而非闪烁高亮 | 连续点击多处时多个闪烁互相干扰，选中态更稳定 |
| 宫格行列可在每版本手动指定 | 镜头数量变化时格子位置不同，写死热区会错位 |
| 资产面板收起态单行平铺两组 | 分组纵向堆叠时收起态只能看到人物图，看不到场景图 |
| 新建批次后立即预填脚本 | 首版生图前就要能核对本批镜头、复制提示词 |
| 终版导出为规定文本格式 | 用户下游流程使用固定脚本格式，JSON 不可直接用 |

## 七、数据模型（localStorage）

```js
{
  projectName: string,
  shots: [{ id, no, camera, scene, duration, desc, dialogue }],
  batches: [{
    id, name, status: 'pending' | 'reviewing' | 'passed',
    draft,                       // 整体意见草稿
    assetIds: [],                // 本批勾选的资产
    annotations: [{ id, shotNo, quote, note }],
    activeV,                     // 当前查看的版本下标
    pendingNos: [],              // 待生图批次的预填镜号
    versions: [{
      id, label,                 // v1 / v2 …
      image,                     // 宫格图 dataURL
      shots, rawText, feedback, createdAt,
      gridCols, gridRows         // 该版本宫格行列（可空 = 自动估算）
    }]
  }],
  assets: [{ id, name, img, type: 'character' | 'scene' }],
  assetDefaultType,              // 新图默认归组
  assetsExpanded,                // 资产面板展开状态
  activeBatchId,
  split                          // 左右分栏比例（%）
}
```

## 八、交付物

| 文件 | 说明 |
| --- | --- |
| `index.html` | 完整单文件应用，浏览器直接打开即用 |
| `分镜审核台-需求文档.md` | 本文档 |
